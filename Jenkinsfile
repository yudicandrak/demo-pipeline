pipeline {
//     agent { docker {image 'node:16.13.2'} }
    agent none
    stages {
        stage('build') {
            steps {
//                 sh 'node --version'
                echo 'check echo . . .'
            }
        }
    }
}
